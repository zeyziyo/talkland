📘 Pronunciation Slot Trainer
Cross-Platform STT/TTS Architecture Design Document

1. 프로젝트 목표

본 프로젝트의 목표는 다음과 같다.

하나의 Python 코드베이스(Flet) 로

데스크탑 앱

모바일(Android) 앱
을 동시에 지원한다.

발음 학습용 슬롯머신 UI를 제공한다.

STT / TTS 기능은 플랫폼 제약을 존중하면서도 동일한 UX를 제공한다.

GitHub Actions를 통해 Android 앱을 자동 빌드할 수 있어야 한다.

2. 핵심 문제 정의
2.1 크로스플랫폼의 현실
항목	Desktop	Android
Python 실행	가능	제한적
pyttsx3	가능	불가
Whisper	가능	불가
ffmpeg	가능	불가
Web API	제한적	가능

👉 모든 플랫폼에서 동일한 STT/TTS 구현은 불가능

3. 설계 원칙 (중요)
원칙 1

UI와 학습 로직은 플랫폼과 무관해야 한다

슬롯 애니메이션

점수 계산

문장 비교

통계

👉 전부 Python(Flet)에서 공통 처리

원칙 2

STT / TTS는 “기능”이 아니라 “플랫폼 서비스”다

구현을 공유하려 하지 않는다

인터페이스만 공유한다

원칙 3

빌드 가능성이 최우선

Android 빌드 실패 = 프로젝트 실패

GitHub Actions 기준으로 설계

4. 전체 아키텍처 개요
┌──────────────────────────────┐
│ Flet UI (Python)             │
│ - 슬롯 UI                   │
│ - 점수 계산                 │
│ - 문장 비교                 │
│ - 통계                      │
└─────────────┬────────────────┘
              │
              ▼
┌──────────────────────────────┐
│ SpeechBackend (Interface)    │
│  - stt()                     │
│  - tts(text, slow)           │
└───────┬───────────┬──────────┘
        │           │
        ▼           ▼
┌──────────────┐  ┌────────────────┐
│ Desktop Impl │  │ Mobile Impl    │
│ (Python)     │  │ (Web API JS)   │
└──────────────┘  └────────────────┘

5. 단계별 개발 계획 (문서화 기준)
✅ STEP 1. 공통 인터페이스 정의

목적

STT/TTS 구현을 교체 가능하게 만든다.

산출물

speech_backend.py

문서 포인트

“왜 인터페이스를 먼저 정의했는가”

“왜 구현을 공유하지 않는가”

✅ STEP 2. DesktopSpeechBackend 구현

대상 플랫폼

Windows / macOS / Linux

기술

STT: Whisper

TTS: pyttsx3

주의

Android 빌드 경로에서 절대 import되지 않도록 설계

문서 포인트

“데스크탑에서만 가능한 이유”

“Android에서 제외되는 이유”

✅ STEP 3. WebSpeechBackend 설계 (모바일)

대상 플랫폼

Android (Flet WebView)

기술

STT: Web Speech API

TTS: speechSynthesis

특징

서버 ❌

API Key ❌

비용 ❌

문서 포인트

“서버가 필요 없는 이유”

“Web API의 보안 / 권한 구조”

✅ STEP 4. 플랫폼 분기 로직
Desktop → DesktopSpeechBackend
Android → WebSpeechBackend


문서 포인트

“런타임 분기 전략”

“빌드 타임과 실행 타임의 차이”

✅ STEP 5. GitHub Actions Android 빌드

목표

git push → APK 자동 생성

필수 조건

Android에서 불가능한 Python 패키지 완전 분리

WebSpeechBackend만 활성화

문서 포인트

“왜 requirements를 분리했는가”

“왜 ubuntu-latest만 가능한가”

6. 기술 선택 요약 (결정 기록)
항목	선택	이유
UI	Flet	Python 기반, 크로스플랫폼
Desktop STT	Whisper	정확도
Desktop TTS	pyttsx3	로컬 실행
Mobile STT	Web API	빌드 가능
Mobile TTS	Web API	서버 불필요
빌드	GitHub Actions	자동화
7. 향후 확장 계획 (문서화 포인트)

발음 정확도 히트맵

단어별 IPA 학습 모드

서버 기반 Whisper (선택 사항)

학습자 레벨별 난이도 조절

8. 이 문서의 의미

이 문서는 단순한 개발 메모가 아니라:

✔ 설계 결정 기록

✔ 미래의 나를 위한 설명서

✔ 협업 시 기준 문서

✔ 포트폴리오용 아키텍처 문서

로 사용할 수 있다.